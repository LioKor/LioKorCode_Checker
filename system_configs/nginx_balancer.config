upstream checker {
       server localhost;
       server ip1;
       server ip2;
}

server {
       listen 8080;
       location / {
               proxy_set_header Host $host;
               proxy_pass http://checker;
       }
}
